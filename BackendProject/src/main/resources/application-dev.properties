# Server Configuration
server.port=3006

# Database Configuration (MySQL Local)
spring.datasource.url=jdbc:mysql://localhost:3306/restaurant
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JSON Formatting
spring.jackson.date-format=dd-MM-yyyy HH:mm:ss

# JWT Security Secret (Utiliser une variable d'environnement en production)
jwt.secret=${JWT_SECRET:SEm108AzjwmNhgc2dzAIardhYdhhQp7baKqPnhKNg08OdmXMcsz01mHSXFhkuNGr}

# Upload de fichiers
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=10MB
upload.path=uploads/plats/

# Logging Configuration
logging.file.name=logs/backend-project.log
# Rolling policy: Keep logs for 7 days
logging.logback.rollingpolicy.max-history=7

# CORS Configuration
app.cors.allowed-origins=http://localhost:5173
